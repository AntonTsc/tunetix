import { FormattedTextPipe } from './formatted-text.pipe';

describe('FormattedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FormattedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
